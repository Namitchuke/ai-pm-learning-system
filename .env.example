# ============================================================
# AI PM Autonomous Learning & Intelligence System
# .env.example — TDD v2.0 §Environment Variables
# Copy this to .env for local development. NEVER commit .env.
# ============================================================

# ---- Authentication (TDD §Security) ----
# Primary API key for programmatic access (generate a strong random string)
API_KEY=your-strong-random-api-key

# Separate secret for cron trigger endpoints
CRON_SECRET=your-strong-cron-secret

# HTTP Basic Auth for dashboard pages
DASHBOARD_USER=admin
DASHBOARD_PASS=your-dashboard-password

# CSRF token signing key (random 32+ char string)
CSRF_SECRET=your-csrf-secret-32-chars-minimum

# ---- Google APIs ----
# Gemini API key — get from aistudio.google.com
GEMINI_API_KEY=your-gemini-api-key

# Google OAuth2 credentials — from Google Cloud Console
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
# Refresh token from one-time OAuth flow (run scripts/get_refresh_token.py)
GOOGLE_REFRESH_TOKEN=your-refresh-token

# Google Drive folder name (do not change unless migrating)
DRIVE_FOLDER_NAME=AI_PM_SYSTEM

# ---- Email ----
# Gmail address to send FROM
SENDER_EMAIL=your-gmail@gmail.com
# Email address to receive the daily digest
RECIPIENT_EMAIL=your-recipient@gmail.com

# ---- Model Configuration (L2-17 — env var for deprecation resilience) ----
# Bulk operations: scoring, summarization, dedup, reteaching
GEMINI_BULK_MODEL=gemini-2.0-flash-lite
# Grading ONLY (higher quality, limited to 100 RPD free tier)
GEMINI_GRADE_MODEL=gemini-2.5-flash

# ---- Application Settings ----
# Environment: development | production
ENVIRONMENT=development
# Log level: DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO
# Port (Render sets $PORT automatically)
PORT=8000
